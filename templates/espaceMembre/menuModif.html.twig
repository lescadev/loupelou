<div id="menuModif">
    <div class="itemMenu">
        <a class="modif" href={{ path('profil') }}>Profil</a>
    </div>
    <div class="itemMenu">
        <a class="modif" href={{ path('profil.password') }}>Modifier mon mot de passe</a>
    </div>
    <div class="itemMenu">
        <a class="modif" href={{ path('profil.email') }}>Modifier mon adresse e-mail</a>
    </div>
    <div class="itemMenu">
        <a class="modif" href={{ path('profil.informations') }}>Modifier mes informations personnelles</a>
    </div>
    {% if (is_granted('ROLE_COMPTOIR') or is_granted('ROLE_PRESTATAIRE')) %}
        <div class="itemMenu">
            <a class="modif" href={{ path('profil.description') }}>Modifier ma description</a>
        </div>
    {% endif %}
    {% if is_granted('ROLE_COMPTOIR') %}
        <div class="itemMenu">
            <br> <a class="modif" href={{ path('profil.transaction') }}>Effectuer une transaction</a>
        </div>
    {% endif %}
    {% if is_granted('ROLE_PARTICULIER') %}
        <div class="itemMenu">
            <a class="modif" href={{ path('profil.pdf') }} target="_blank">Imprimer ma fiche adhérent</a>
        </div>
    {% endif %}
</div>
